name: HA Calendar2Image
version: "0.8.0"
slug: calendar2image
description: Transform ICS calendar feeds into customizable images with optional extra data sources - perfect for e-ink displays and dashboards
url: https://github.com/jantielens/ha-calendar2image
arch:
  - aarch64
  - armv7
init: false
startup: application
boot: auto
ports:
  3000/tcp: 3000
ports_description:
  3000/tcp: Web server port for API endpoints
map:
  - addon_config:rw
